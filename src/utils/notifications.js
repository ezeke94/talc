// Notification utilities removed. Expose a no-op setup function to avoid import errors.
export async function setupNotifications(/* currentUser */) {
  // Intentionally no-op after removal of notification features.
  return null;
}
